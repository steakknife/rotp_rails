This will test Google Authenticator.

<%= form_for :user, url: validate_user_path(@user), method: :post do |f| %>
  <%= render 'form_errors' %>
  <%= label_tag(:rotp_code, 'Enter code from Google Authenticator') %>
  <%= f.text_field :rotp_code %>
  <%= f.submit %>
<% end %>
